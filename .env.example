# 数据库（必填，否则 /api/albums 和抓取会 500）
# Neon：请在控制台用「Pooled connection」连接串（主机名含 -pooler），例如：
# postgresql://USER:PASSWORD@ep-xxx-pooler.region.aws.neon.tech/neondb?sslmode=require
# 注意：不要加 channel_binding=require（PgBouncer 不支持）
DATABASE_URL=postgresql://user:pass@ep-xxx-pooler.region.aws.neon.tech/neondb?sslmode=require

# 网易云链接解析（抓取功能必填）
# 本地：先运行 npx NeteaseCloudMusicApi（端口如 3002），再填 NETEASE_API_URL=http://localhost:3002
# 线上：填公网 API 地址，如 https://xxx.up.railway.app
NETEASE_API_URL=http://localhost:3002

# Clerk 登录（必填，用于 /lyrics-wall 保护）
# 在 https://dashboard.clerk.com 创建应用后，API Keys 页面复制
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_xxx
CLERK_SECRET_KEY=sk_test_xxx

# Clerk 登录/注册页路径（用于 header 按钮跳转，建议填写）
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up

# Clerk 登录后重定向（可选，默认 /lyrics-wall）
# CLERK_AFTER_SIGN_IN_URL=/lyrics-wall
# CLERK_AFTER_SIGN_UP_URL=/lyrics-wall

# 人生歌单 seed：设为网易云歌单 ID 时，npm run db:seed 会将该歌单曲目导入「人生歌单」分类（未登录可见）
# SEED_LIFE_PLAYLIST_ID=123456789

# Vercel Blob 存储（可选，Vercel 部署会自动注入）
# 本地不需要；Vercel 部署时在项目 → Storage → 创建 Blob Store 即可
# 如未配置，网易云解析会直接使用原始 CDN 链接，但手动上传功能不可用
# BLOB_READ_WRITE_TOKEN=vercel_blob_rw_xxx
